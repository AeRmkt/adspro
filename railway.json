{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run db:generate"
  },
  "deploy": {
    "startCommand": "npm run build --workspace=packages/types && npm run build --workspace=packages/utils && npm run build --workspace=apps/api && cd apps/api && npm run db:migrate && node dist/server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
